import {
    Component,
    ElementRef
} from '@angular/core'

{{#if coloredComponent}}
import { ColorElement } from '../core/color-element'
{{else}}
import { Element } from '../core/element'
{{/if}}

@Component({
    selector: 'hg-{{component.selector}}',
    exportAs: '{{component.name}}',
    templateUrl: '{{component.template}}',
    styleUrls: ['{{component.style}}']
})
export class {{component.name}} extends {{#if coloredComponent}}ColorElement{{else}}Element{{/if}} {

    constructor(elementRef: ElementRef) {
        super(elementRef)
    }

}
